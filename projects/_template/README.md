# Project: TITLE

## Question
One-sentence scientific goal.

## Data location (local only)
Put machine-specific paths in an untracked `paths.local.md` (do not commit secrets).

## Entry points
- `scripts/01_run_analysis.*` — main pipeline
- Calls reusable code from `../../imaging/` or `../../scrnaseq/` etc.

## Outputs
Where figures/tables are written (folder is gitignored if large).

## Status
- [ ] Scripts imported
- [ ] Paths made portable
- [ ] Minimal example runs on laptop
