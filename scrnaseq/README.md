# scRNA-seq

| Subfolder | Use |
|-----------|-----|
| `integration/` | Batch correction / dataset integration (Harmony, scVI, Seurat integrate, …) |
| `analysis/` | Clustering, markers, trajectories, plots |

Prefer saving **scripts + small example objects**; large `.h5ad` / `.rds` stay on disk (gitignored).
