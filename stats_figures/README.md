# Statistics & figures

Publication-oriented plotting and stats helpers.

| Subfolder | Use |
|-----------|-----|
| `boxplots/` | LiAng MATLAB boxplot style — see `makeCombinedFigures.m` |
| `templates/` | `cleanSubplot`, `figurePosition`, `savefigure` |

Agent skill: `/figure-boxplot` (`.cursor/skills/figure-boxplot/SKILL.md`).
